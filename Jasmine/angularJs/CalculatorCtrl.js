(function () {

    'use strict';

    angular
        .module('app', [])
        .controller('calculatorController', function ($scope) {
            // var vm = this;
            $scope.message = 'I am a simple calculator';
            // vm.result = 0;
            // vm.firstNumber = 0;
            // vm.secondNumber = 0;
            // vm.addNumber = addNumber;

            // function addNumber() {
            //     vm.result = vm.firstNumber + vm.secondNumber;
            // }

        });
})();

